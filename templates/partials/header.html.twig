<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-3">
  <div class="container-fluid">
    <a href="/" class="navbar-brand">CIEL IR notre 1er projet Symfony</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
      <span class="navbar-toggle-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a href="{{ path('home') }}" class="nav-link"><i class="bi bi-house"></i> Home</a>
        </li>
        <li class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"><i class="bi bi-files"></i> Articles</a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
            <a href="{{ path('article_index') }}" class="dropdown-item"><i class="bi bi-list-ul"></i> Liste des articles</a>
            {% if app.user %}
              <div class="dropdown-divider"></div>
              <a href="{{ path('article_new') }}" class="dropdown-item"><i class="bi bi-file-plus"></i> Ajouter un article</a>
            {% endif %}
            <div class="dropdown-divider"></div>
            <a href="{{ path('article_by_price') }}" class="dropdown-item"><i class="bi bi-search"></i> Recherche article par prix</a>
          </div>
        </li>
        <li class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"><i class="bi bi-inbox"></i> Catégories</a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
            <a href="{{ path('category_index') }}" class="dropdown-item"><i class="bi bi-list-ul"></i> Liste des catégories</a>
            {% if app.user %}
              <div class="dropdown-divider"></div>
              <a href="{{ path('category_new') }}" class="dropdown-item"><i class="bi bi-plus-square"></i> Ajouter une catégorie</a>
            {% endif %}
            <div class="dropdown-divider"></div>
            <a href="{{ path('article_in_cat') }}" class="dropdown-item"><i class="bi bi-search"></i> Recherche article par catégorie</a>
          </div>
        </li>
        <form class="form-inline my-2 my-lg-0" method="get" action="{{ path('app_search') }}">
          <input name="mot_cle" class="form-control form-control-sm mr-sm-2" type="search"
                 placeholder="Recherche par nom article" aria-label="Search">
          <button class="btn btn-outline-dark btn-sm my-2 my-sm-0" type="submit">Rechercher</button>
        </form>
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a href="{{ path('admin_user_index')}}" class="nav-link"><i class="bi bi-pencil-square"></i> Administration</a>
          </li>
        {% endif %}
      </ul>
        <ul class="navbar-nav mr-auto">
        {% if app.user %}
          <li class="nav-item dropdown ">
            <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button"
               aria-haspopup="true" aria-expanded="false">{{ app.user.username }}</a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="#"><i class="bi bi-pencil"></i> Modifier mon profil</a>
              <a class="dropdown-item" href="{{ path('security_logout') }}"><i class="bi bi-box-arrow-left"></i> Déconnexion</a>
            </div>
          </li>
        {% else %}
          <li class="nav-item ">
            <a href="{{ path('security_login') }}" class="nav-link"><i class="bi bi-box-arrow-right"></i> Connexion</a>
          </li>
          <li class="nav-item ">
            <a href="{{ path('security_registration') }}" class="nav-link"><i class="bi bi-pencil-square"></i> Inscription</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>